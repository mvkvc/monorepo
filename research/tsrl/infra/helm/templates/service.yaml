apiVersion: v1
kind: Service
metadata:
  name: {{ include "tsrl_cluster.fullname" . }}
  labels:
    {{- include "tsrl_cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tsrl_cluster.selectorLabels" . | nindent 4 }}

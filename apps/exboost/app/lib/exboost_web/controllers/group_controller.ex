defmodule ExboostWeb.GroupController do
  use ExboostWeb, :controller

  # def create(conn, %{"key" => _key} = _params) do
  #   conn
  # end

  # def delete(conn, %{"key" => _key}) do
  #   conn
  # end
end
